# Module-level .gitignore (kept minimal)
# Most rules are handled by the repository root .gitignore

# Module build output
/target/

# IDE/editor files commonly created in the module
*.iml
.vscode/
.idea/

# Keep the Maven wrapper jar tracked (do not ignore .mvn/wrapper/maven-wrapper.jar)
# If you need module-specific exceptions, add them below
